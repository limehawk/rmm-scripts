name: workstation_info.vbs
description: Displays a popup with OS, computer, hardware, and network info
language: VBScript
tags:
- Monitoring
- Windows
runAs: SYSTEM_USER
timeout: 15
readme: |
  Purpose
  -------
  Displays a popup message box showing system information to the end user.
  Designed to be triggered from the RMM tray icon for user self-service so a
  person can read their own machine name, serial, and specs without opening a
  ticket.

  Usage Notes
  -----------
  - Connects to the local WMI service (hard-fails if unreachable)
  - Queries OS, computer, CPU, memory, BIOS, and network adapter info
  - Renders a formatted summary via MsgBox (popup) or WScript.Echo (console)
  - Output host: wscript.exe renders a MsgBox popup; cscript.exe prints to
    stdout. The tray trigger uses wscript (popup) by default.

  Requirements
  ------------
  - Windows 10/11 with Windows Script Host enabled
  - No special privileges required (local WMI read)
  - Takes no configuration inputs
favourite: false
shared: false
scriptId: '259059265290493952'
lastPushedChecksum: sha256:3897e294e0f6e0a1
lastPushedMetaChecksum: sha256:28741e94b073d133
