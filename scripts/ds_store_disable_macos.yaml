name: ds_store_disable_macos.sh
description: Prevents macOS from creating .DS_Store files on network shares
language: Bash
tags:
- Mac
- Maintenance
runAs: SYSTEM_USER
timeout: 15
readme: |
  Purpose
  -------
  Prevents macOS from creating .DS_Store files on network shares. These files contain folder metadata and can clutter network drives, causing issues for non-Mac users accessing shared folders.

  Usage Notes
  -----------
  - Sets DSDontWriteNetworkStores to true via defaults command
  - Displays confirmation message
  - Logout or reboot is required for changes to take effect

  Requirements
  ------------
  - macOS operating system
  - No special privileges required (runs as current user)
favourite: false
shared: false
scriptId: '259059088395722752'
lastPushedChecksum: sha256:6a32e8ee9e2fd422
lastPushedMetaChecksum: sha256:86e98ba6e03ccf87
